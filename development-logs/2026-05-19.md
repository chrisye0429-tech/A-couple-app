# 2026-05-19 开发日志

## 今日完成

- 创建 `docs/` 项目文档文件夹。
- 创建 `development-logs/` 开发日志文件夹。
- 创建根目录工作说明文件 `AGENTS.md`。
- 编写首版需求说明、技术方案、设计规范、开发步骤和验收标准。
- 明确当前阶段只搭建项目管理结构，不开发 App 功能代码。
- 初始化 Expo + TypeScript 跨平台 App 工程。
- 创建基础 App 入口、主题色文件和首屏占位界面。
- 安装项目依赖并生成依赖锁定文件。
- 运行 TypeScript 类型检查，结果通过。
- 新增 `docs/运行说明.md`，记录安装、检查和启动方式。
- 完成首页与基础导航：包含首页、文件夹、手账、共享四个入口。
- 新增手账编辑器静态预览，为后续拖拽编辑能力做界面准备。
- 新增 `docs/首页与导航说明.md`，记录当前页面结构。
- 建立本地手账编辑器原型的数据结构，包含页面、信纸、图片、文字、贴纸、工具栏和图层。
- 将编辑器拆分到 `src/features/journalEditor/` 模块，便于后续继续开发。
- 新增 `docs/手账编辑器数据结构.md`，记录编辑器结构和后续方向。
- 实现编辑器“添加文字元素”：点击工具栏“文字”后创建默认文字块，自动选中，并同步显示在画布、图层和属性面板。
- 新增文字后保存状态会切换为“本地草稿”。
- 实现编辑器“添加贴纸元素”：点击工具栏“贴纸”后创建默认贴纸，自动选中，并同步显示在画布、图层和属性面板。
- 安装 `@react-native-async-storage/async-storage`，用于本地保存手账页面结构。
- 新增 `storage.ts`，封装本机读取和保存手账页面的方法。
- 实现“保存到本机”：点击后保存当前 `JournalPage`，再次进入编辑器时优先读取本机保存内容。
- 安装 `expo-image-picker`，用于本地相册图片选择。
- 实现编辑器“本地图片选择”：点击工具栏“图片”后请求相册权限，选择图片后创建图片元素并显示到画布。
- 扩展图片元素结构，支持保存图片 URI。
- 在 `app.json` 中补充相册权限说明。
- 实现元素拖拽移动：图片、文字、贴纸均可在画布中按住拖动，移动后更新元素坐标并切换为“本地草稿”。
- 实现元素缩放：选中图片、文字或贴纸后，可通过“缩小 / 放大”按钮调整尺寸，并保持在画布范围内。
- 根据模拟器反馈优化文件夹：支持新建后重命名、重命名已有文件夹、删除文件夹。
- 优化手账文字元素：画布中文字只显示文字本身，不再显示底色和边框。
- 优化元素叠加：点选元素时自动提升层级，便于重叠元素继续编辑。
- 实现删除选中元素：图片、文字、贴纸均可删除，删除后切换为“本地草稿”。
- 优化排版体验：拖动元素时自动识别画布边缘、画布中心和其他元素边缘/中心，距离 6px 内自动吸附并显示辅助线。

## 遇到问题

- 使用官方 Expo 模板下载命令时，权限自动检查曾超时；已改为手动创建标准工程骨架并正常安装依赖。
- npm 安装过程中出现第三方旧依赖提醒，但安装成功，当前不影响项目运行。
- Expo 开发服务启动时，本机缺少 Watchman，Metro 因文件监听数量限制退出；App 工程本身的类型检查已通过。
- 首页与基础导航开发过程中未遇到代码错误，TypeScript 类型检查通过。
- 手账编辑器数据结构和页面结构开发过程中未遇到代码错误，TypeScript 类型检查通过。
- 当前电脑已安装 Watchman，版本为 `2026.05.18.00`。
- Codex 沙盒不能写入 Watchman 所需的 macOS `LaunchAgents`，因此沙盒内仍无法完整启动 Metro；普通本机终端应使用 `npm start` 运行。
- 用户授权后，Watchman 已能监听当前项目目录，Expo / Metro 已在 `19005` 端口成功启动。
- 根据 Expo 提示对齐依赖版本：`expo-status-bar` 调整到 `~55.0.6`，`react-native` 调整到 `0.83.6`。
- 添加文字元素开发过程中未遇到代码错误，TypeScript 类型检查通过。
- 添加贴纸元素开发过程中未遇到代码错误，TypeScript 类型检查通过。
- 本地保存页面结构开发过程中，首次类型检查发现新增样式未定义；已补齐样式并通过 TypeScript 类型检查。
- 本地图片选择开发过程中未遇到代码错误，TypeScript 类型检查通过。
- 元素拖拽移动开发过程中未遇到代码错误，TypeScript 类型检查通过。
- 元素缩放开发过程中未遇到代码错误，TypeScript 类型检查通过。
- 模拟器反馈修复过程中未遇到代码错误，TypeScript 类型检查通过。
- 智能对齐吸附开发过程中，首次类型检查发现吸附结果类型推断过窄；已补充明确类型并通过 TypeScript 类型检查。

## 明日待办

- 确认首版需求文档是否符合产品方向。
- 继续确认 `React Native / Expo + TypeScript` 是否作为正式技术路线。
- 继续细化本地手账编辑器的可编辑交互。
- 下一步可开始做文字内容编辑，或继续完善文件夹与页面的持久化保存。
- 使用 `npm run start:codex` 或普通终端 `npm start` 进行真机或模拟器预览。

## 风险提醒

- 手账编辑器、同步编辑和分享权限是后续复杂度最高的部分，需要分阶段推进。
- 不建议一开始同时开发编辑器、账号、同步和分享，容易导致项目不稳定。
