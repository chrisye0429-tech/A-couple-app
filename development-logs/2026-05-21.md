# 2026-05-21 开发日志

## 今日完成

- 集成 20 款免费可商用字体配置，新增 `src/features/materials/fonts.json`。
- 字体库包含思源黑体、思源宋体、霞鹜文楷、得意黑、江城圆体、优设标题黑、猫啃珠圆体、阿里巴巴普惠体、庞门正道标题体、点字倔云等字体，并补充 10 款适合手账的开源中文字体。
- 扩展字体数据模型，新增字体来源、授权说明、预览文案、备用字体、建议行高和上线前复核状态。
- 素材管理器已自动合并 `fonts.json`，支持分页读取字体、生成字体预览配置、下载并注册预览字体。
- 字体注册增加去重逻辑，同一字体重复选择时不会重复注册。
- 更新 `docs/素材管理系统.md` 和 `docs/技术方案.md`，记录字体库、字体预览流程和后续接入方式。
- 已确认用户通过终端成功安装 `expo-font` 和 `expo-file-system`。
- 当前安装版本为 `expo-font@55.0.8` 和 `expo-file-system@55.0.21`。
- 将素材管理器从预留字体适配器升级为默认接入 `expo-font`，用户选择字体预览时会真正注册字体。
- 将字体缓存升级为 `expo-file-system` 文件级缓存，字体会下载到本机缓存目录 `couple-journal-fonts`，避免重复网络加载。
- 兼容历史缓存：旧的字体网络 URL 缓存不会继续复用，会重新下载成本地文件后再注册。
- 初始化 20 种手账背景模板，包含 8 种纯色、6 种纹理和 6 种图案。
- 扩展背景素材数据结构，新增本地绘制模式、主色、辅助色、强调色、平铺/缩放适配和自定义标记。
- 编辑器新增背景模板面板，点击“背景”工具后可切换背景。
- 画布背景改为本地绘制，支持网格、点阵、横线、牛皮纸、水彩、复古、碎花、波点、条纹、格子、云朵和星星效果。
- 新增自定义纯色背景原型，支持常用色板和 `#RRGGBB` 颜色输入。
- 修复模拟器里看不到字体选择入口的问题：文字编辑面板已新增 20 款字体选择按钮，选择后会触发字体下载和注册。
- 修复模拟器里看不到新增本地贴纸的问题：贴纸面板已改为读取 `stickers.json`，支持 20 个分类切换，每类显示前 20 个贴纸。
- 文字元素数据结构新增 `fontId`、`fontFamily` 和 `fontName`，本地保存时可保留用户选择的字体。
- 贴纸元素数据结构新增 `stickerId`、`sourceUrl`、`provider` 和 `status`，方便后续替换成真实图片素材。
- 将画布内贴纸元素改为透明 PNG 风格：优先使用素材图片地址渲染，不再显示彩色圆形底色；占位素材才用透明文字符号兜底。
- 搭建素材 CDN 部署目录，新增 `assets/stickers/`、`assets/fonts/`、`assets/backgrounds/` 和 `assets/version.json`。
- 新增素材清单生成脚本 `scripts/build-assets-manifest.mjs`，可计算素材 hash 和文件大小并更新 `version.json`。
- 新增 CDN 发布目录准备脚本 `scripts/prepare-assets-cdn.mjs`，可生成 `dist/assets-cdn/`。
- 新增 GitHub Pages 工作流 `.github/workflows/deploy-assets.yml`，用于发布素材静态目录。
- 新增 App 端 `assetCdnManager`，支持读取远程 `version.json`、对比本地素材 hash，并增量下载更新素材。
- 新增 `docs/素材CDN部署方案.md`，记录 GitHub Pages + jsDelivr 的部署和版本检查流程。
- 暂时不切换 Flutter，继续按 React Native / Expo 路线推进。
- 开始整理手账编辑器代码结构，新增 `src/features/journalEditor/EditorPanels.tsx`。
- 将回退/前进、底部工具栏、贴纸面板、文字编辑面板、图层列表和属性面板从 `EditorScreen.tsx` 拆出。
- `EditorScreen.tsx` 从约 3500 行降到约 3000 行，当前仍保留画布、手势、背景和对齐逻辑，后续分阶段继续拆。
- 更新 `docs/手账编辑器数据结构.md` 和 `docs/开发步骤.md`，记录当前代码拆分进度和后续整理顺序。
- 继续拆分画布相关组件，新增 `src/features/journalEditor/JournalCanvas.tsx`。
- 将手账画布、纸张背景绘制、元素渲染、四角操作点、拖拽和双指缩放手势从 `EditorScreen.tsx` 中移出。
- `EditorScreen.tsx` 进一步降到约 2300 行，当前主要负责页面状态、历史记录、本地保存和业务操作回调。
- 画布拆分后，背景模板预览继续复用 `PaperBackground`，避免背景绘制逻辑重复。
- 清理 `EditorScreen.tsx` 中已迁移到 `EditorPanels.tsx` 和 `JournalCanvas.tsx` 的旧样式。
- 清理后 `EditorScreen.tsx` 进一步降到约 1700 行，当前样式只保留主页面、背景模板面板和保存区域仍在使用的部分。
- 拆分背景模板面板，将背景模板、自定义纯色输入和背景预览移动到 `EditorPanels.tsx` 的 `BackgroundPalette`。
- `EditorScreen.tsx` 进一步降到约 1460 行，当前更专注于页面状态、历史记录、本地保存和数据更新逻辑。
- 检查 `EditorScreen.tsx` 和 `EditorPanels.tsx`，当前未发现未使用样式。
- 拆分编辑器纯数据逻辑，新增 `src/features/journalEditor/editorModel.ts`。
- 将手机端页面尺寸适配、背景数据转换、自定义颜色校验、文字/贴纸/图片元素创建、元素复制和贴纸显示兜底移入 `editorModel.ts`。
- `EditorScreen.tsx` 进一步降到约 1100 行，当前主要保留状态协调、历史记录、保存、图片选择和页面操作回调。
- `EditorPanels.tsx` 和 `JournalCanvas.tsx` 已复用 `editorModel.ts` 中的贴纸显示和最小元素尺寸规则，减少重复逻辑。

## 遇到问题

- 此前安装 `expo-font` 和 `expo-file-system` 时，当前环境无法访问 npm 镜像站；用户后续已在终端完成安装，我已检测确认依赖存在，并完成正式接入。
- 部分国内字体可商用，但公开 CDN 直链路径需要上线前逐项复核，已在 `fonts.json` 中标记为 `pending-review`。
- 背景模板当前使用本地组件绘制，不依赖 Canvas 新库或 9-patch 图片；后续如果要做更复杂水彩/纸张纹理，可以再引入图片素材或专门绘图库。
- 当前 CDN 地址仍为占位地址，正式发布前需要替换为真实 GitHub 用户名和仓库名。
- TypeScript 类型检查通过。
- 本次编辑器组件拆分后，已再次通过 TypeScript 类型检查。
- 本次画布组件拆分后，已再次通过 TypeScript 类型检查。
- 本次旧样式清理后，已再次通过 TypeScript 类型检查。
- 本次背景模板面板拆分后，已再次通过 TypeScript 类型检查。
- 本次纯数据逻辑拆分后，已再次通过 TypeScript 类型检查。

## 明日待办

- 在 iOS 和 Android 模拟器上验证字体动态注册、字体缓存命中和中文字体格式兼容性。
- 处理字体下载失败时的界面提示，保证弱网情况下仍能使用备用字体。
- 在模拟器中确认文字面板字体按钮、贴纸分类按钮和新增贴纸显示是否符合预期。
- 在模拟器中检查 20 种背景模板的视觉效果、文字可读性和滚动面板布局。
- 后续可把自定义纯色升级为真正的取色器组件。
- 替换素材 CDN 占位地址，完成 GitHub Pages 设置，并用真实 CDN 地址测试 App 启动检查。
- 继续整理 `EditorScreen.tsx` 中的历史记录、智能对齐计算和页面操作回调。
- 后续可考虑为 `editorModel.ts` 增加单元测试，验证旧页面尺寸迁移、元素复制和颜色校验逻辑。

## 风险提醒

- 字体文件较大，尤其是中文全量字体；后续需要考虑字体子集、懒加载和弱网提示。
- 不同平台对 `woff2`、`ttf`、`otf`、`ttc` 的支持可能不同，正式上线前需要在 iOS 和 Android 模拟器逐项验证。
- 当前纹理和图案是轻量原型绘制，适合验证交互和审美方向；如果要达到更真实纸张质感，需要补充高质量可商用纹理素材。
- 画布手势逻辑已经移入 `JournalCanvas.tsx`，后续如果继续改拖拽、缩放和四角操作点，应优先在该文件内处理。
